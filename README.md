# Overview:
Uses Flask and Matplotlib to compare and plot the amortization of base vs extra mortgage payments.

## To Do
Investigate migrating form matplotlib to PyplotLite to reduce container size
📌 SUMMARY: What you should add next

A solid real-world pipeline looks like this:

Lint YAML

Black/Flake8/Pylint

Security scan (bandit)

Run unit tests

Hadolint Dockerfile scan

Build Docker image

Trivy vulnerability scan

Push to Docker Hub

Deploy to on-prem VM (via SSH or runner)

Notify